server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: ${ALLOWED_ORIGINS:*}
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
          filters:
            - RewritePath=/api/(?<segment>.*), /$\{segment}
        
        - id: assessment-service
          uri: lb://assessment-service
          predicates:
            - Path=/api/tests/**, /api/topics/**
          filters:
            - RewritePath=/api/(?<segment>.*), /$\{segment}

        - id: scoring-service
          uri: lb://scoring-service
          predicates:
            - Path=/api/results/**, /api/chat/**
          filters:
            - RewritePath=/api/(?<segment>.*), /$\{segment}
        
        - id: payment-service
          uri: lb://payment-service
          predicates:
            - Path=/api/payments/**
          filters:
            - RewritePath=/api/(?<segment>.*), /$\{segment}
        
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE}

management:
  endpoints:
    web:
      exposure:
        include: health, gateway
  endpoint:
    health:
      show-details: always
